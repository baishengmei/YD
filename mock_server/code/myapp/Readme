1）. 参数定义
	1. type为float时，value表示整数部分的范围或者float的指定值，value1表示小数部分的位数；
	2. type为int、float等时，可以为空白、指定值、取值范围。
	3. 枚举型enum，在配置页面输入后，是字符串形式，并以逗号隔开，如"apple, red, green".
		注意：实际请求中也是字符串形式，并以逗号隔开；
			  枚举中的项不可重复；
	4. data类型包括三种，data、time、data_time。其中data形式："1929-04-10",time形式："12:06:12",data_time形式："1929-04-10 12:06:12".目前data只校验数据格式，并未校验月份小于12，天数小于31，特殊没有31天以及28和29天的情况，以及年份不超过今天。不支持写入状态。
	5. Address包括country、province、city、region和zip。其中zip的校验为：是否为6位数字，其他项的校验为：是否为汉字。
	6. 规则中保存数字时，以字符串形式保存。
